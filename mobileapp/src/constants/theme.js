export const colors = {
  primary: '#c0392b',
  primaryDark: '#922b21',
  background: '#f9f5f3',
  surface: '#ffffff',
  border: '#e8dcd9',
  text: '#2c2c2c',
  textMuted: '#7a7a7a',
  success: '#2e7d32',
  danger: '#c0392b',
  warning: '#b7791f',
  locked: '#fff3e0',
};

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

// Shared sizing so every form control (text input, picker, date field, searchable
// picker) renders at the same height/border/radius instead of drifting apart as
// each component grew its own numbers.
export const control = {
  borderWidth: 1,
  borderRadius: 10,
  paddingHorizontal: 12,
  paddingVertical: 12,
};
